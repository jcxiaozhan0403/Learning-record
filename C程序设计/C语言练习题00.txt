选择题
1.下列可合法地用作用户标识符的是（    B   ）。
A.p#d            B.Int            C.a-b           D.define
2．以下不是C语言基本数据类型的是（   D    ）。
A.字符型        B.单精度实型     C.整型          D.构造类型
3.C语言程序由（   C   ）组成。
A.子程序        B.主程序和子程序     C.函数          D.过程
4.实数是用指数形式输出时是按规范化的指数形式输出的。因此当指定将实数1234.5按指数形式输出时，则正确的输出形式是（   D   ）。
A.1234.5        B.1.2345e3     C.12.345e2          D.123.45e1
5.要输出换行符，正确的转义字符是（   B   ）。
A.\\         B.\n        C.\t          D.\’
6.标准输入/输出函数原型在头文件（  A   ）中。
A.stdio.h         B.math.h        C.time.h          D.string.h
7.printf()函数的格式说明符%8.3f是指（  D  ）。
A.输出场宽为7的浮点数，其中小数位为3，整数位为4
B.输出场宽为11的浮点数，其中小数位为3，整数位为8
C.输出场宽为8的浮点数，其中小数位为3，整数位为5
D.输出场宽为8的浮点数，其中小数位为3，整数位为4
8. 对下面的for循环，循环体执行的次数是（    ）。
for ( i = 1; i <= 10 ; i = i + 3 ) ;
A.1         B.2        C.3          D.4
9.下面的循环执行完后，a为（    ）。
#include "stdio.h"
int main()
{
	int a;
	for(a=1;a<5;a=a+2){
		a=2*a;
	}
	printf("%d",a);
}
A.10         B.15        C.20          D.25
10.设有变量说明：float x=4.0,y=4.0;。下面使x为-6.0的表达式是（    ）。
A.x-=y*2.5         B.x/=y+9        C.x*=y-6          D.x+=y+2
11.printf()函数的格式说明符中，要输出字符串应使用下面哪一个说明符（    ）。
A.%d         B.%f       C.%s          D.%c
12.以下哪一项不是算法的特点（    ）。
A.确定性      B.有零个或多个输入     C.无效性        D.有一个或多个输出
13. 有输入语句：scanf(“a=%d,b=%d,c=%d”,&a,&b,&c);为使变量 a的值为1，b的值为3，c的值为2，则正确的数据输入方式是（  ）。
A.132↙		   B.1,3,2↙        C.a=1 b=3 c=2↙ 		  D.a=1,b=3,c=2↙
14. 以下哪个数学函数的作用是求某数的绝对值（  ）。
A.sqrt()		   B.fabs()	  	   C.pow()		         D.exp()
15. 设有定义：char s[12]=＂string＂;则printf (＂%d\n＂,strlen(s));的输出是（    ）。
A.6         B.7         C.11         D.12
16. for循环中，表达式1的作用是（    ）。
A.循环变量赋初值    B.循环条件    C.改变循环变量的值   D.什么作用都没有
17.C语言中，下列（     ）函数可用于两个字符串的连接。
A.stelen         B.strcat         C.strcpy         D.strcmp
18. 若有定义：int a[3][4],则数组a中一共有（   ）个元素。
A.3         B.4         C.7         D.12
19. C语言中各种类型的数据其实决定了占用内存的字节数。float占（  ）。
A.一字节      B.二字节       C.四字节     D.八字节
20. 表达式” 3 > 2 ”的值是(      )。
A.true       B.非零值        C.0             D.1

填空题
1.下面程序段循环执行的次数是             。
int a=0,j=10;
	for(;j>3;j--){
		a++;
		if(a>3)break;
	}
2.下列程序段输出结果是                              。
#include "stdio.h"
int main()
{
	int i,a[10];
	for(i=0;i<10;i++){
		a[i]=3*i+1;
	}
	for(i=9;i>=0;i--,i--){
		if(a[i]/2)
			printf("%5d",a[i]);
	}
}
3.将代数式-2ab+b-4ac改写成C语言的表达式：                               

编程题
1.第51页课堂练习3，求分段函数的值。
2.使用for实现：随机产生20个[20,40]之间的整数，求其平均值并输出，输出结
果保留小数后2位。 
